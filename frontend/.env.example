# API base URL — only needed when running a standalone build (not proxied by Vite)
# For local dev the vite.config.js proxy handles /api → http://localhost:5000
# VITE_API_URL=https://your-api-domain.com
